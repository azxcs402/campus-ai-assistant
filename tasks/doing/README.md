# tasks/doing · 进行中

存放**正在执行**的任务文件。

## 使用规则

1. 任务从 `tasks/inbox/` 移入本目录时，把任务文件中的「当前状态」改为 `doing`；
2. 任务文件内可追加执行记录（进度、遇到的问题、调整理由），但**不得删除**任务目标/完成标准/不允许修改的范围等原始约束；
3. 完成标准全部满足并通过自查后，移动到 `tasks/review/`，状态改为 `review`；
4. 同一时间建议只做 1 个任务（并行实现任务需在任务文件注明依赖关系）。

> 当前无进行中任务。
