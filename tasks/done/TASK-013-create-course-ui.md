# TASK-013 自定义课程创建入口

- 状态：done
- 负责 Agent：Codex
- 范围：前端课程创建交互

## 完成标准

- 登录后可从页面直接创建自定义课程。
- 课程名称不能为空，学期为可选输入。
- 创建成功后两个课程选择器同步刷新并选中新课程。
- 创建失败时显示可读错误。
- 使用现有 `POST /api/v1/courses` 接口，不新增后端依赖。

## 实际结果

- 在学习任务表单增加“新建课程”按钮。
- 创建时校验课程名称，学期支持留空。
- 创建成功后刷新任务和资料课程选择器，并自动选中新课程。
- 失败时显示中文错误提示。
- 后端测试：`7 passed`；前端语法检查通过。
