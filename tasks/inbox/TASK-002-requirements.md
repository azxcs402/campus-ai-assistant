# TASK-002 · 需求定稿与验收标准

- 编号：TASK-002-requirements
- 当前状态：`ready`
- 依赖：建议在 TASK-001（用户调研）之后执行；TASK-006（原型验证）的结论会再次回填本文档对应验收标准

## 任务目标

把设计思维结论与真实调研证据固化为**可执行、可验收**的需求文档：定稿 [docs/02-requirements.md](../../docs/02-requirements.md) 的功能需求（FR-01~17）、非功能需求（NFR）与验收标准（AC），并保证与设计文档、任务编号全局一致。

## 背景和上下文

- 初始化轮已给出需求草案：版本分层（基础/标准/特色）、FR-01~17、NFR-01~09、AC 初步条目（见 02 文档）；
- 草稿中的优先级基于待验证假设；TASK-001 调研与 TASK-006 原型测试都可能改变需求细节，本任务负责在这些输入就绪后把文档从"草稿"推进到"定稿基线 v1.0"；
- 范围红线（02 文档第 5 节）与任务书一致，定稿不得悄悄扩大范围。

## 需要修改的文件

- `docs/02-requirements.md`：定稿 FR/NFR/AC；删除"草稿"状态；标注与证据的对应；
- 若需求变化牵动其他文档，同步更新并记录：
  - `docs/01-design-thinking.md`（转化映射表）、`docs/04-api-spec.md`（接口是否承载新需求）、`docs/05-database.md`（字段是否足够）、`docs/06-test-plan.md`（用例与 AC 对应）；
- `docs/vibe-log/`：追加需求定稿日志。

## 不允许修改的范围

- 不得修改 `README.md`；
- 不得把"待验证假设"写成已验证事实（除非有 TASK-001/006 的证据并注明）；
- 不得新增超出任务书范围的版本内容（Live2D/语音/QQ/Telegram/复杂本地文件操作等仍为可选）；
- 不得删除 FR 编号后复用（编号一经发布永久保留，作废需标注 Deprecated）。

## 完成标准

1. FR-01~17 每条具备：唯一编号、名称、版本归属、描述、可测的 AC（1 条以上）；
2. NFR-01~09 可验证（有测量方式或检查项）；
3. 需求与证据可追溯：关键需求标注来源（设计思维结论编号 I1~I7/J/P/G 或调研证据）；
4. 版本边界清晰：基础版条目满足任务书基础版 8 项门槛（含 ≥3 个 HTTP API）；
5. 交叉检查通过：02 与 01（第 6 节映射表）、04、05、06 编号引用一致，无孤儿编号。

## 测试方法

- 可测性评审：每条 AC 能写出对应测试用例（用例模板见 [docs/06-test-plan.md](../../docs/06-test-plan.md)）；
- 编号一致性脚本/人工核对：所有引用 FR-x 的文档编号真实存在；
- 交叉检查：由另一 Agent 或人工按 [AGENTS.md](../../AGENTS.md) 第 6 节清单复核。

## 交付物

- 定稿 `docs/02-requirements.md`（基线 v1.0，注明定稿日期）；
- 需求→证据/设计结论追溯表（文档内）；
- 受影响文档的同步更新；
- `docs/vibe-log/` 日志。

## 当前状态

`ready`（待执行）
