# TASK-003 · 系统架构与技术选型

- 编号：TASK-003-system-architecture
- 当前状态：`ready`
- 依赖：TASK-002 需求定稿后执行（架构必须服务需求）；结果供 TASK-004/005 使用

## 任务目标

在 [docs/03-architecture.md](../../docs/03-architecture.md) 概念架构基础上完成**技术选型与架构定稿**：确定前端/后端/数据库/向量库/AI 接入的具体方案，明确模块边界、数据流与部署形态，产出依赖清单与选型理由，满足 NFR-09（可替换、无多余依赖）。

## 背景和上下文

- 03 文档当前为逻辑架构（组件职责、数据流、安全边界），**技术栈刻意未预设**，附录 A 留待本任务填写；
- 约束来自任务书与需求：课程演示级单机部署、基础版先闭环、AI 组件接口化可替换、密钥只留后端；
- 参考 EchoBot 仅借鉴思路，不得复制实现；选择任何框架/库必须说明它服务于哪条需求。

## 需要修改的文件

- `docs/03-architecture.md`：填写附录 A 技术选型；把"概念架构（草稿）"更新为定稿；补充分层实现说明与部署形态；
- 选型如影响数据模型或接口，同步提示 TASK-004/005 对应文档（`docs/05-database.md`、`docs/04-api-spec.md`）；
- `docs/vibe-log/`：追加选型决策日志（含备选方案与取舍理由）。

## 不允许修改的范围

- 不得修改 `README.md`；
- 不得引入与需求无关的技术（禁止"顺手加"框架/中间件/容器编排等重设施）；
- 不得让密钥/供应商凭据进入前端或文档（[AGENTS.md](../../AGENTS.md) 第 5 节）；
- 本任务只做**设计与选型决策**，不实现业务功能代码（原型验证例外见 TASK-006）。

## 完成标准

1. 技术选型明确并记录理由：前端方案、后端方案、数据库、向量检索承载方式、LLM/Embedding 接入（含无网降级）；
2. 每个选型能追溯到需求/NFR（如 NFR-06 移动端、NFR-09 可替换、NFR-05 降级）；
3. 03 文档完成定稿：包含模块图/数据流更新、目录结构草案、环境变量清单（只列键名不列值）、启动与演示形态；
4. 依赖清单评审通过：无多余依赖（对照 [AGENTS.md](../../AGENTS.md) 第 5.2 条）；
5. 输出架构决策记录（ADR 或等价物，至少覆盖：技术栈、RAG 组件、认证方案、AI 降级策略）。

## 测试方法

- 架构评审：另一 Agent 或人工按完成标准与需求可追溯性核验；
- 可行性冒烟（可选）：用最小脚手架验证选型可运行（如"hello world"级，不实现业务逻辑）；
- 检查密钥/敏感配置未出现在任何文档与提交中。

## 交付物

- 定稿 `docs/03-architecture.md`（含附录 A 技术选型）；
- 架构决策记录（可放在 03 文档内或 `docs/` 下独立文件）；
- 依赖清单与理由表；
- `docs/vibe-log/` 日志。

## 当前状态

`ready`（待执行）
