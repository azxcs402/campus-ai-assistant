# 06 · 测试计划（Test Plan）

> 文档编号：06 ｜ 状态：计划（随 TASK-002/003/005 的实现细化并回填执行结果）
> 覆盖：功能测试、异常/边界测试、AI 输出验证、用户体验测试、接口测试。需求/验收编号来自 [02-requirements.md](02-requirements.md)。
>
> **实现与执行现状（2026-09-10，基线 `codex/standard-feature-expansion` @ `222fa02`）**：后端自动化测试 **21 条 = 15 + 6**
> - `backend/tests/test_api.py`（15 条）：基础闭环与异常/边界（见 §2、§3 标注）
> - `backend/tests/test_standard_features.py`（6 条）：任务编辑与 `completed_at`、课程详情/改名/安全删除、角色管理与跨用户权限、按课程与日期/逾期统计、会话私有性、离线 AI 评估集（见 §2.1、§4.3）
>
> Codex 报告 **21 passed**；`node --check frontend/app.js` 与 `git diff --check` 通过。**本文档的自动化执行结果引用 Codex 报告，尚未由文档 Agent 复跑。** RAG（AT-01~AT-06）与用户体验测试（§5）**未执行（计划中/待本人执行）**。

## 1. 测试目标与策略

- **目标**：保证基础版闭环可演示、标准版健壮可测、特色版 RAG 输出可信可溯源；
- **分层策略**：接口自动化为主（后端契约，FR-08/AC）、端到端冒烟为辅（核心用户旅程）、AI 输出用离线评估集验证（NFR-08）；
- **环境**：开发环境 + 演示环境（种子数据）；AI 依赖可在无外网情况下跳过并标记（NFR-05 降级验证）；
- **数据**：每套环境使用独立数据库与测试账号；**禁止使用真实个人信息与真实密钥**。

## 2. 功能测试用例（按 FR 抽样，完整用例在实现任务中展开）

| 用例 ID | 关联需求 | 测试步骤（摘要） | 预期结果 |
| --- | --- | --- | --- |
| FT-01 | FR-01/AC-01 | 注册 → 登录 → 刷新页面 → 登出 → 再登录 | 会话保持符合 AC-01；**登出接口未实现（计划中）**，前端仅清除本地 token |
| FT-02 | FR-02 | 新建→编辑→标记完成→筛选→删除（含二次确认） | **已实现/已自动化**：`test_task_can_edit_all_fields_and_tracks_completion`（编辑含 `course_id`、`completed_at` 写入与清空）；删除有二次确认；筛选由 `GET /tasks?course_id=&status=` 支持，**前端暂无任务筛选控件** |
| FT-03 | FR-03 | 创建课程→进入课程详情→改名→删除 | **已实现/已自动化**：`test_course_details_update_and_safe_delete`（详情含任务/资料数、改名、非空课程删除 409、空课程 204）；**邀请码加入未实现（计划中）** |
| FT-04 | FR-04 | 给课程造 3 条资料数据 → 前端列表 | 已实现（资料列表随所选课程联动，空状态有引导） |
| FT-05 | FR-05 | 上传支持格式 → 观察状态流转到 ready | **状态机未实现（计划中）**：`parse_status` 恒为 `queued`，前端显示"已保存（解析待接入）"；上传白名单/大小限制已自动化 |
| FT-06 | FR-06/07 | 连续对话 5 轮 → 刷新 → 打开历史会话继续 | 消息落库已自动化（`test_conversation_persists_messages`）；**多轮上下文与历史会话切换 UI 未实现（计划中）** |
| FT-07 | FR-09 | 造 1 小时后截止任务 → 查看首页提醒 | **部分实现**：`#upcomingList` 展示 7 天内未完成任务（`urgent` ≤24h 高亮、逾期显示"已逾期"），已自动化（`test_upcoming_tasks_returns_due_items`）；**无推送/通知（计划中）** |
| FT-08 | FR-10/17 | 造多课程多状态任务 → 查看统计与图表 | **已实现/已自动化**：`test_course_date_and_overdue_statistics`（按课程分组、日期范围、逾期计数、422 日期校验）；前端为 CSS 条形图（无图表库）；**趋势图未实现（计划中）** |
| FT-09 | FR-11 | student/teacher/admin 三个账号分别访问资源 | **已实现/已自动化**：`test_admin_role_management_and_cross_user_permissions`（仅管理员可列出/改角色、禁止改自身、非成员课程 403、改他人任务 404、管理员可改）；**教师专属权限未实现（计划中）** |
| FT-10 | FR-16 | 大任务 → 拆解 → 编辑部分子任务 → 确认 | **未实现（计划中）**：无 `/tasks/{id}/split`、`/tasks/batch` 与 `parent_task_id` |

### 2.1 标准版功能的自动化用例（`backend/tests/test_standard_features.py`，6 条）

| 用例 | 断言要点 |
| --- | --- |
| `test_task_can_edit_all_fields_and_tracks_completion` | PATCH 改标题/课程/截止/优先级/备注/状态均生效；转 `done` 写 `completed_at`，转回清空 |
| `test_course_details_update_and_safe_delete` | 详情含 `task_count`；改名生效；**有任务的课程删除 409**；空课程删除 204 |
| `test_admin_role_management_and_cross_user_permissions` | 普通用户 `GET /users` 403；管理员可改他人角色；非成员访问课程 403；改他人任务 404；管理员可改他人任务 |
| `test_course_date_and_overdue_statistics` | `/stats/overview?course_id=` 的 total/completed/overdue/完成率；`/stats/courses` 分组；日期过滤；非法日期 422；from>to 422 且 detail 文案 |
| `test_conversation_is_private_but_admin_can_review` | 他人会话消息 404；管理员可读 |
| `test_offline_ai_evaluation_set_uses_mock_provider_and_persists` | 6 条离线评估样例在 mock provider 下全部通过 `score_output`，且消息落库数为 `样例数 × 2`（详见 [ai-offline-eval.md](ai-offline-eval.md)） |

> 注：另有 `backend/tests/test_api.py` 的 15 条用例覆盖基础闭环与异常边界（重复注册 409、非法课程名 422、非法日期 422、非法扩展名/超 20MB 400、资料越权 404、任务不存在 404、无效 Base URL 422、AI 401/超时/断网安全降级、provider 转发字段、未登录 401 等）。

## 3. 异常与边界测试（FR-08 及专项）

| 用例 ID | 场景 | 预期 |
| --- | --- | --- |
| ET-01 | 必填缺失 / 非法格式 / 超长输入 | **实现为 422 + `{"detail": ...}`**（FastAPI 默认）；前端 `request()` 已把 `detail` 数组映射为逐条 `msg` 文本；`{code,details}` 统一结构属**计划中**（见 [04-api-spec.md](04-api-spec.md) §1.1） |
| ET-02 | 访问他人任务/会话/资料（改 ID 越权） | 403/404，不泄露资源存在性（AC-08-3）——**已自动化** |
| ET-03 | 未登录 / 令牌过期访问受保护接口 | 未登录 401 **已自动化**；**令牌过期/伪造无专项用例（计划中）** |
| ET-04 | 上传不支持格式 / 超大文件 / 空文件 | 非法扩展名与超 20MB **已自动化（400）**；空文件未覆盖 |
| ET-05 | 删除有子任务的父任务 / 删除有资料的课程 | **课程**：非空课程 409 阻止删除**已自动化**；**父任务自关联未实现**（`parent_task_id` 计划中，故不适用） |
| ET-06 | 重复点击提交（任务、消息） | 前端按钮禁用（`withBusy`）已实现；**服务端幂等/防重未实现（计划中）** |
| ET-07 | AI 网关超时/不可用 | **实现为统一文案"AI 服务暂时不可用…"（HTTP 201）**，非 502/504；401/超时/断网三类已由 mock 自动化覆盖，非 AI 功能不受影响（NFR-05） |
| ET-08 | 空课程 / 无资料 / 空会话等空状态 | 空状态引导已实现（前端占位文案）；无专项自动化 |
| ET-09 | 数据库/文件存储异常 | 无专项用例（计划中） |

## 4. AI 输出验证（特色版重点）

> 原则：AI 回答必须可溯源、不编造（FR-14/15）；无相关内容如实告知。
>
> **实现现状（2026-09-10）**：RAG **未实现**，因此 §4.1 的引用命中率/拒答率与 §4.2 的 AT-01~AT-06 **均未执行（计划中）**；当前可执行的 AI 离线检查见 §4.3（关键词/结构/一致性检查，**不代表模型质量**）。

### 4.1 离线评估集（Golden Set）— 面向 RAG，计划中

- 建立每课程 10–20 条「问题 → 期望覆盖点 + 期望引用资料」的评估集；
- 指标：**引用命中率**（回答引用的 source 是否真实存在于资料）、**内容相关度**（人工 1–5 分）、**拒答正确率**（资料中确实没有时是否正确说明"未找到"）；
- 门槛（初步，TASK-006 后校准）：引用命中率 ≥ 95%，无来源时拒答率 100%，相关度均分 ≥ 4。

### 4.2 专项用例

| 用例 ID | 场景 | 预期 |
| --- | --- | --- |
| AT-01 | 问资料中明确存在的内容 | 回答正确且引用来源与资料一致（AC-14/15） |
| AT-02 | 问资料中不存在的内容 | 回答"资料中未找到"，不编造（AC-14-2） |
| AT-03 | 引用定位抽查 | 点击来源能定位到该 chunk 的章节/页码（AC-15-1） |
| AT-04 | 多资料课程提问 | 引用可来自多份资料，来源均可追溯 |
| AT-05 | 删除/重传资料后再提问 | 不再引用已删除内容，重传后使用新内容（AC-13-2/3） |
| AT-06 | 恶意/超长/重复骚扰问题 | 长度与频率约束生效（429），不影响他人 |

> **AT-01~AT-06 全部未执行（计划中）**：均依赖 RAG 实现（检索、引用、拒答），当前无对应能力。

### 4.3 当前可执行的 AI 离线检查（已实现）

- `backend/offline_eval.py` 提供 6 条样例（E-01~E-06）与 `score_output()`（非空、必需关键词命中、行动结构标记、不含服务错误文案）；
- 由 `test_offline_ai_evaluation_set_uses_mock_provider_and_persists` 以 **mock provider** 驱动，无外网、无真实 API Key，并验证消息落库；
- **自动化通过 ≠ 模型质量达标**；人工双人 0–4 分评分**尚未执行**（详见 [ai-offline-eval.md](ai-offline-eval.md)）。

## 5. 用户体验测试（对应设计思维 Test 阶段）

- 执行载体：`tasks/inbox/TASK-006-prototype-validation.md`；
- 内容：低保真/可点击原型测试 6 名以上目标学生，任务、指标、预判问题与修改回路见 [01-design-thinking.md](01-design-thinking.md) 第 5 节；
- 出口条件：任务完成率 ≥ 80%、无阻断级问题、至少两轮修改复测；结果回填 02 文档的 AC 与本文档 4.1 门槛。
- **执行状态（2026-09-10）：未执行（待本人执行）**。真实用户调研与原型验证暂不执行，相关结论仍为待验证假设（[01-design-thinking.md](01-design-thinking.md)）；Codex 已完成的浏览器验证（任务编辑/完成率/课程详情/375px 无溢出）属**功能走查**，不等同于用户测试。

## 6. 接口测试（配合 [04-api-spec.md](04-api-spec.md)）

- TASK-005 定稿 OpenAPI 后生成契约测试：每个端点覆盖 200/400/401/403/404 主分支；
- 关键流程端到端冒烟：登录 → 建课 → 建任务 → 上传资料 → 问答（rag）→ 查看统计；
- 测试账号矩阵覆盖三角色；测试不依赖真实 AI 可用性（mock 网关可切换）。

## 7. 回归与发布检查

- 基础版冒烟（8 项）：登录、建任务、标完成、资料列表、普通对话、历史会话、即将截止、统计展示；
- 每版发布前跑通：功能用例 + 异常用例 + 冒烟；特色版加跑 AI 评估集；
- 记录执行结果到任务文件与 `docs/vibe-log/`，禁止"未测即声称通过"（[AGENTS.md](../AGENTS.md) 第 5 节）。
