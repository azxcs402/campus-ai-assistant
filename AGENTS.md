# AGENTS.md — 两个 Agent 共同遵守的开发规则

> 本文档面向本项目中的两个协作 Agent：
>
> - **需求分析与规划 Agent（DeepSeek）**：负责用户调研、设计思维、需求分析、架构与任务拆分，产出 `docs/` 与 `tasks/` 中的文档。
> - **实现 Agent（Codex）**：负责根据知识库与任务文件执行原型验证、系统设计落地与代码实现。
>
> 双方在动手前必须先阅读本文档以及相关的 `docs/` 文档，任务执行过程中必须遵守以下规则。

## 1. 项目与工作流总则

1. 本项目是《系统设计与实践》课程项目，流程必须遵循设计思维：共情 → 定义 → 构思 → 原型 → 测试 → 系统设计。所有代码实现必须能追溯到 `docs/` 中的需求与设计文档。
2. 参考项目 [EchoBot](https://github.com/KdaiP/EchoBot) **只能作为技术参考**，不得直接复制其代码作为课程作业提交；本项目必须有独立的校园学习场景、需求分析、系统设计和实际修改。
3. **文档先行、任务驱动**：任何实质性开发都先进入 `tasks/` 生成任务文件，按 `inbox → doing → review → done` 流转，而不是直接动手写代码。
4. 阶段推进必须与 `docs/01-design-thinking.md` 保持一致：设计思维尚未得到原型/访谈验证的结论一律标记为「待验证假设」，不允许当作既成事实。

## 2. 分支与提交规则

1. 分支命名：功能/任务分支使用 `deepseek/<工作主题>` 或 `codex/<工作主题>` 语义化命名，例如 `deepseek/project-knowledge`、`codex/task-005-api-design`。
2. 提交信息使用约定式前缀（`docs:` / `feat:` / `fix:` / `refactor:` / `test:` / `chore:`），并简述改动内容。
3. 一次提交只做一件逻辑上的事；提交前运行 `git status` 检查不误提交无关文件。
4. **禁止提交**：API Key、`.env`、数据库凭据、真实个人信息、任何密钥或令牌。`.gitignore` 已覆盖常见敏感文件，新增敏感文件时必须在提交前加入忽略规则。
5. 每个任务完成后必须提交并推送，保证远端分支可恢复、可协作。

## 3. 文档维护规则

1. `README.md` **由人工维护，两个 Agent 都不得修改**。Agent 产出的知识入口是 `docs/00-project-overview.md`。
2. `docs/` 中 00–06 号文档互相引用必须一致：
   - 需求编号（FR-xx）、任务编号（TASK-xxx）、状态词（`ready`/`doing`/`review`/`done`）、角色、版本分层（基础版/标准版/特色版）等术语全局统一。
   - 修改任何一个文档后，必须检查引用它的其他文档是否需要同步更新（见第 6 节一致性检查清单）。
3. 文档中的「待验证假设」必须保留标记，直到对应的调研任务（如 TASK-001、TASK-006）给出证据后，由负责该任务的 Agent 在文档中更新为「已验证」或「已证伪」，并注明证据来源与日期。
4. 每次 Agent 工作结束，必须在 `docs/vibe-log/` 中追加一条日志：记录本轮做了什么、改动了哪些文件、关键决策、遗留问题。

## 4. 任务流转规则（tasks/）

- `tasks/inbox/`：待办任务，任务文件初始状态统一为 `ready`。
- `tasks/doing/`：正在执行的任务；开始执行时移动任务文件并改状态为 `doing`，同时把拆出的子项记录在任务文件中。
- `tasks/review/`：已完成实现、等待交叉检查或人工复核的任务，状态为 `review`。
- `tasks/done/`：通过复核、完成标准全部满足的任务，状态为 `done`。
- 任务文件的「不允许修改的范围」是硬边界：例如不得修改 `README.md`、不得把「待验证假设」写成事实、不得扩大版本范围。若确需突破，必须先在任务文件中记录理由并经人工确认。

## 5. 代码实现规则（针对实现 Agent，规划 Agent 起草）

1. 只实现当前任务「完成标准」要求的范围，禁止顺手扩大范围或引入无关功能（Live2D、语音、QQ/Telegram、复杂本地文件操作等为可选功能，不得影响基础系统交付）。
2. 不引入没有必要的技术与依赖；每个依赖必须能在需求/架构文档中找到理由。
3. 遵循 `docs/03-architecture.md` 的分层边界：前端只通过 HTTP API 与后端交互；AI/RAG 能力只通过后端暴露，任何密钥不得进入前端或提交到仓库。
4. AI 相关输出必须可溯源：RAG 回答必须能展示引用来源；知识库无相关内容时必须如实告知，不允许编造。
5. 提交代码前必须跑通文档 `docs/06-test-plan.md` 中对应任务要求的测试，并把结果写回任务文件。

## 6. 一致性检查清单（每次交付前执行）

- [ ] 所有 Markdown 文件语法正确（标题层级、表格、代码块完整）。
- [ ] 术语统一：角色名、版本分层、状态词、编号体系（FR / API / TASK）无冲突。
- [ ] 文档间引用（相对链接、任务编号、需求编号、API 路径）真实存在。
- [ ] 没有把「待验证假设」写成事实；没有虚构已完成功能。
- [ ] 没有引入密钥、`.env`、真实个人信息。
- [ ] 本次工作已记录到 `docs/vibe-log/`。
- [ ] `README.md` 未被修改。
- [ ] 工作树干净、分支正确、提交已推送。
